# Shared Logic

This directory contains reusable predicates and flow logic
that are shared across multiple packages within this project.

The design and core logic were originally sourced from the
`jackfromeast/class-pollution-all` pack.